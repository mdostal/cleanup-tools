step_id: write-brief
status: completed
timestamp: "2026-08-11"
notes: >
  BucketRule shape: (extensions: frozenset, filename_pattern: str|None, bucket: str), first-match-wins
  ordered list — screenshots rule listed before photos rule, mirrors bash if/else without a
  second mechanism. MasterPath (path, backed_up) and search_roots added to schema now even
  though unused until the reclaim story. Config I/O goes through OSAdapter (read_file/
  write_file/resolve_home), not raw pathlib/open(), for testability symmetry with test_adapters.py.
artifacts: []
