step_id: backend-implement
status: completed
timestamp: "2026-08-11"
notes: >
  Additive OSAdapter extension: list_subdirs() + find_dirs() (with proper find-style pruning,
  tested against nested node_modules-in-node_modules) added to base.py + 6 new adapter tests
  (92 total now). cli.py (argparse, extensible subparsers, [project.scripts] entry point) +
  commands/survey.py (all 6 sections from survey.sh) created. Smoke-tested against the real
  home directory on this machine — all 6 sections produced sensible real output (disk usage,
  dir sizes, node_modules total ~15.9GB across 36 dirs, 492 screenshots found). No CLI-specific
  tests yet — that's the next step.
artifacts:
  - src/cleanup_tools/adapters/base.py
  - src/cleanup_tools/cli.py
  - src/cleanup_tools/commands/__init__.py
  - src/cleanup_tools/commands/survey.py
  - pyproject.toml
