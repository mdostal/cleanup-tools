step_id: fix-cycle-implement
round: 1
status: completed
timestamp: "2026-08-12"
notes: >
  Fixed both major bugs: thumbnail route now catches Image.DecompressionBombError alongside
  UnidentifiedImageError/OSError, returning the existing graceful 404 instead of a raw 500
  (reproduced with a real 20000x20000 PNG). approve/reject/undo/plan_sort/plan_reclaim now
  catch TimeoutError from adapter.file_lock contention, returning 503 (routes) or a flash-
  message redirect (plan routes) instead of a raw 500 (reproduced by holding a real fcntl
  lock externally). Also fixed the double-transition/single-undo minor finding in
  queue.py's set_status() by skipping duplicate consecutive history entries. All three
  fixes verified red (reverted, confirmed new test fails/bug reproduces) then green
  (restored, confirmed pass). 245 -> 248 tests pass.
artifacts:
  - src/cleanup_tools/ui/routes.py
  - src/cleanup_tools/queue.py
  - tests/test_ui_routes.py
  - tests/test_queue.py
