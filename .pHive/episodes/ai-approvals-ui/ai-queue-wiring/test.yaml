step_id: test
status: completed
timestamp: "2026-08-12"
notes: >
  19 new tests in test_ai_wiring.py, mocked provider only. Centerpiece: cap-enforcement-
  by-exact-call-count test (cap < candidate count, asserts provider call count not result
  count -- catches a call-all-then-filter bug that a result-count assertion alone would
  miss). Also covers mixed success/failure batches, correctly-shaped queue entries, and
  route-level no-API-key graceful degradation. The one-real-API-call manual verification
  criterion inherited from the ai-provider-layer story remains skipped for the same reason
  (no live key in this environment). 301 -> 320 (implement) -> 327 (post-fix-cycle) tests
  pass.
artifacts:
  - tests/test_ai_wiring.py
